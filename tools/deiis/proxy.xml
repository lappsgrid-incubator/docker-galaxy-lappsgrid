<tool id='cmu.squad.proxy' name='Squad Proxy' version='1.0.0'>
  <description>proxy a service at another URL</description>
  <command interpreter='lsd'>proxy.lsd $url $input $output</command>
  <inputs>
  	<param name='url' type='text' label='Service URL'/>
    <param name='input' type='data' format='lif' label='Input' />
  </inputs>
  <outputs>
    <data name='output' format='lif' />
  </outputs>
  <help>A proxy service that can invoke other services at the user supplied URL.</help>
</tool>